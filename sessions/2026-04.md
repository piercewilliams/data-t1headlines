# Session Archive — April 2026

---

### 2026-04-15 — Snowflake table paths confirmed; author playbook upgrade path identified

**Commit:** `25d94c6`

Snowflake/Sigma orientation with Rocky Rhodes + Chad Bruton (2026-04-15 1:30pm CDT):
- `story_traffic_main` (MCC presentation → Tableau reporting): O&O page views by story/market/date — primary source for article-level traffic. Primary table for author playbook upgrade.
- `dynamic_story_metadata` (MCC presentation): author_name, URL, SEO metadata, keywords, taxonomies — join on story ID. Author attribution handled here.
- Author playbook upgrade path: join `dynamic_story_metadata` (author_name) to `story_traffic_main` (O&O PV) on story ID. Blocked on GitHub→Snowflake connection (Chad Bruton setting up).
- Sigma is NOT the right path — Sigma is just a reporting UI on top of Snowflake. Connect GitHub directly to Snowflake instead (Chad to help).
- CONTEXT.md updated: open item added for O&O + syndication PV data layer via Snowflake.

---

### 2026-04-14f — Code quality hardening: exception handling, pyproject fix, README

**Scope:** Cross-repo quality audit pass touching data-headlines specifically.

- `download_tarrow.py` — wrapped `json.loads(sa_json)` on `GOOGLE_SERVICE_ACCOUNT_JSON` env var with `try/except json.JSONDecodeError`; previously an unhandled crash on malformed secret (e.g. missing base64 decode step)
- `generate_experiment.py` — pre-computed `_lift_display` before the `not_significant` f-string to guard `lift:.2f` format against `None` (lift=None when stat_a==0); also replaced bare `except Exception: pass` swallowing chart failures with a stderr log of slug + exception message
- `generate_grader.py` — narrowed `except Exception: pass` in `load_history()` to `(json.JSONDecodeError, OSError)` with a stderr warning; narrowed broad date-parse exception to `except ValueError` — both were silently discarding errors
- `ingest.py` — replaced silent `except Exception: pass` in opportunity map check with a log of check_id + exception message
- `pyproject.toml` — fixed `target-version = "py312"` → `"py311"` to match CI Python version; mismatch meant ruff could not flag 3.12+ syntax that would fail on CI Python 3.11
- `README.md` — updated quality suite: `python3.11 -m py_compile` replaces `ast.parse` check (py_compile actually enforces 3.11 f-string restrictions); added explicit note to always use `python3.11` not local Python (which may be 3.14+)

**Quality gate:** 21 tests pass under Python 3.11 before and after these changes.

---

### 2026-04-09: Grader hardening + weekly auto-ingest pipeline

**Grader criteria updates (generate_grader.py):**
- `char_count` label corrected to "Character count (SN 70–90 / AN 90–120)"; `_char_count()` rewritten to validate against 70–120 range (valid on at least one platform) with contextual notes
- `no_questions` added as scored quality criterion (obj, weight=1); `_no_questions()` detects trailing `?`
- `no_vague_wtk` moved from `llm` → `obj` method; `_no_wtk()` added (regex, p=3.0e-6 badge); LLM prompt reduced from 5 to 4 criteria; `_LLM_KEYS` updated accordingly
- `_ACRONYMS` expanded ~30→~55 entries (SEC, EPA, DOJ, NYT, NAACP, ESPN, HBO, etc.)
- Warning text in grader HTML corrected: removed "vague WTK" from LLM criteria list

**Cross-repo headline standards audit:**
- WTK p-value resolved: more recent run (p=3.0e-6, n=213) takes precedence over prior run (p=0.046). Survives Bonferroni at k=7. Finding is significant; "avoid WTK" stays firm.
- Featured placement exception removed from all standards docs (csa-content-standards: headlines.md, platform-apple-news.md, api/reference.json) — 0% featuring rate in current data, no support for the exception.

**Weekly auto-ingest pipeline (new):**
- `download_tarrow.py` — downloads 2026 Google Sheet as XLSX via Drive API export endpoint; reuses existing `GOOGLE_SERVICE_ACCOUNT_JSON` secret; 2025 static, never re-downloaded
- `.github/workflows/weekly_ingest.yml` — cron Monday 8pm CDT (01:00 UTC Tuesday); compares new xlsx to HEAD before triggering regeneration (skips if unchanged); commits updated xlsx + docs/ + data/ when changed
- `generate_site.py` — `_write_build_summary()` block added at end; writes `data/build_summary.json` with 8 tracked metrics (fb_wtk_sn_rank, fb_q_sn_rank, fb_sn_n, notif_len_best_ctr, sn_top_share, sn_channel_rows, an_median_hl_len, sn_median_hl_len)
- `update_snapshots.py` — reads build_summary.json, date-stamps it, appends to data/weekly_snapshots.json; idempotent on same-day reruns

**Integrity pass fixes:**
- xlsx was not in `git add` — change-detection would re-fire every week regardless of data changes; fixed
- `google-auth-httplib2` removed from workflow (code uses `google.auth.transport.requests`, not httplib2)
- `requests` added to requirements.txt
- REFERENCE.md: Tarrow cadence corrected "start of every month" → "EOD every Monday"
- README: grader criteria table updated; file reference + data files + outputs tables updated for new pipeline scripts; quality suite corrected (ruff removed, not installed)
- Smoke tests: AST parse tests added for download_tarrow.py and update_snapshots.py → 19 tests, all pass

**Prerequisite pending:** Share Tarrow 2026 sheet with `pierce-tools-service-account@pierce-tools.iam.gserviceaccount.com`. Message sent to Chris Tarrow 2026-04-09.

---

### 2026-04-08c: Full Sarah Price alignment + SmartNews channel data + code hardening

Exhaustive alignment pass against Sarah Price's written feedback. New analysis: nature/wildlife dual-headline formula guidance (SN overperforms, AN underperforms — opposite platform routing); 0% featuring investigation (content type is the constraint, not section tagging or formula — no editorial lever exists); head-to-head headline examples per formula (top 3 organic vs. bottom 3 for 5 formula types). Integrated new 28-column SmartNews article-level data: per-channel views + recommended_view; 86.3% of T1 views from Top feed. Fixed MSN sheet name mismatch. Gated all MSN references behind `SHOW_MSN_TILE`. Fixed author playbook regression. Added `PENDING_HIGH_ANALYSES` guardrail. Style guide tier sort. Code hardening: guards, escaping, MSN try/except. 16/16 smoke tests pass.

---

### 2026-04-08b: Sarah Price tile feedback — governance + pipeline aligned

Cuts: tiles 4 (Views/Reading Depth), 6 (Featuring Reaches Non-Subscribers), 7 (Topic Predicts Featuring), MSN Formula Divergence tile (`SHOW_MSN_TILE = False`), Push send-time section. Restructured Formula × Topic tile. Exploratory Tracker→ANP join: 32% match rate, 355 articles, 0% featuring rate across all verticals vs 1.2% ANP baseline — featuring is not a lever for this content type. Trendhunter vertical mapping confirmed by Sarah Price (author = proxy for vertical). All governance and pipeline updated. 16/16 smoke tests pass.

---

### 2026-04-08a: GitHub Actions daily grader + Run Now button

Retired unreliable local crontab. Built `.github/workflows/grader.yml`: runs daily at 10am CDT; commits updated HTML + history JSON via `GITHUB_TOKEN`. Added Run Now button to grader UI: passcode 8812 gates access; fine-grained GitHub PAT (actions:write) stored in localStorage; calls GitHub Actions dispatch API. Secrets needed: `GROQ_API_KEY` and `GOOGLE_SERVICE_ACCOUNT_JSON` (base64-encoded — use `~/.credentials/pierce-tools.json`; encode with `base64 -i ~/.credentials/pierce-tools.json | pbcopy`).

---

### 2026-04-06: Headline Grader built + delivered

Requirements gathered from Sara Vallone + Sarah Price at C&P Weekly and prior sessions, refined by Pierce. Built `generate_grader.py` — daily headline grader pulling from Sara Vallone's live Google Tracker sheet (Sheet ID `14_0eK46g3IEj7L_yp9FIdWwvnuYI5f-vAuP7DDhSPg8`).

**14 criteria in 4 tiers:**
- Rule-based: character count, formula detection (here's/WTK/question), keyword presence, all-caps, DYM opener, article lead, subject leads, informational flags
- Groq LLM (`llama-3.3-70b-versatile`): active voice, lead burial, curiosity gap, accuracy, vague WTK
- Weighted score 0–100%; 0.4s sleep between LLM calls (Groq free-tier rate limit)

**Infrastructure:** 30-day rolling history persisted to `docs/grader/history.json`; history strip rendered on grader page. Cron at 10am Chicago via `run_grader.sh`; env vars in `~/.grader_env` (GROQ_API_KEY, GITHUB_TOKEN, GOOGLE_SERVICE_ACCOUNT_FILE). "Headline Grader" added to `_NAV_PAGES`; all 5 nav pages regenerated.

**Hardening pass:** Fixed `curiosity_gap`→`curiosity` key mismatch (LLM results were never scoring curiosity), dead `if w==0: pass` block in `_badge()`, invalid strptime `%-m/%-d` formats, HTML-escaping in headline cards, latent NameError in `generate_site.py` (`MSN_VID_SPORTS_P_STR` called `_fmt_p` before defined). Added `generate_grader.py` to smoke test AST suite (now 16 tests). Removed "T1 Headlines" brand badge sitewide.

**Quality suite:** `pytest` 16/16 ✓, `ruff` clean ✓, grader dry-run passes ✓.

**Delivered:** Site link sent to Sara Vallone + Sarah Price 2026-04-06.

**Also committed at C&P Weekly:** Individual performance tracking — identify writers who consistently beat tool suggestions. Will add per-author breakdown to grader output; author field already in Sara's Tracker sheet. Pending: criteria refinement feedback from Sara/Sarah.

---

### 2026-04-01: ANP data integration + Finding 8

Sarah Price surfaced need for full Apple News universe data. Chris Tarrow set up weekly automated exports from Apple News Publisher; first drop (Jan–Feb 2026, 8 publications, 420K rows / 80K articles) arrived same day.

**ANP exploratory analysis (8 angles):** profiled → 10,929 2026-published news-pub articles. Featured vs. non-featured headline signals (BH-FDR), section × featuring rates, top-decile views signals, notification traffic amplification, subscriber ratio by section, Us Weekly signals, cross-pub topic performance, active time vs. views.

**Findings wired:**
- **Finding 6 — Featuring Reaches Non-Subscribers:** Featured articles: 47% non-subscriber audience; non-featured: 3%, even among top-quartile performers (5%). p=6.6e-277. Featuring is audience acquisition, not a traffic boost.
- **Finding 7 — Topic Predicts Featuring More Than Formula:** Weather featured at 41%, Sports 1.4% (28× gap). Question format: 2× featuring lift (χ²=28.6, p=8.78e-08). Business situation/event vs. named-person: 2× (directional).
- **Finding 8 — ANP Bottom-Performer Patterns (Sarah Price request):** Three structural failure modes: missing section tag (47.5% bottom-quintile, p=9.9e-36), local sports without featuring (27.2% bottom, p=5.4e-102), national wire content (35.5% bottom vs 0.59 local median, p=5.3e-19).

**Infrastructure:** `anp_data/` created (gitignored), `_load_anp()` + `_anp_analysis()` added, `.gitignore` created, 5 new `_COL_TOOLTIPS`. Two new playbook tiles: Section Tagging, Local vs. National. Playbook at 4 tiles.

**Not wired (below findings bar):** Us Weekly (100% named-person, no formula comparison possible). Notification traffic amplification (selection vs. amplification ambiguous; only 2/1,726 notif articles had majority notif traffic).

---

### 2026-04-02: March ingest + exhaustive cross-platform analysis (5 new findings)

**Data ingested:** March Tarrow drop — Apple News +434 rows → 1,569 YTD; Notifications +144 → 503; MSN restructured to clean "MSN" sheet (845 rows Jan–Mar); SmartNews now monthly-aggregated by domain (514 rows, not article-level); Yahoo/AOL split (Yahoo 1,043 rows, -51% from prior — AOL separated). Fixed SmartNews column rename (`date`→`month`). Re-enabled MSN (`EXCLUDE_MSN=False`).

**Key data notes:** Repo root `Top Stories 2026 Syndication.xlsx` is the Feb build (old "MSN (minumum 10k PV)" sheet format). April 2 file with clean "MSN" sheet was processed via ingest.py but not retained. If regenerating from scratch, re-download from Tarrow's Sheet.

**Exhaustive analysis run (~20 hypothesis classes):** headline length × featuring, active time × featuring, brand-level featuring rates, notification time-of-day, notification headline length, channel-level CTR, SmartNews category H1/H2 trends, saves × featuring, ANP section × formula interaction, ANP publication-level featuring, SmartNews formula performance, cross-platform formula matrix, notification word-level signals (10 tests, BH-FDR corrected), ANP formula × topic combinations.

**Five additions to the site (8 → 13 findings):**

1. **MSN Formula Divergence** — direct declarative headlines 4.84× better than any structured formula on T1 news MSN (p=2.9e-13). Questions/here's/explainers all significantly below baseline. Opposite of Apple News playbook.

2. **Formula × Topic Interaction** — "here's" and question format lift Apple News featuring ONLY on weather content: `heres×weather` 70.6% (n=102), `question×weather` 56.7% (n=67). For all other topics, formula barely predicts featuring (11–18%, near baseline). Topic is the primary driver; formula is a secondary signal within weather/emergency coverage only.

3. **SmartNews Cross-Platform Formula Trap** — question (0.423 rank, p=3.4e-6, n=918) and "what to know" (0.371, p=3.0e-6, n=213) actively hurt SmartNews performance. "Here's" (0.543, p=0.038) is the only formula above baseline on both platforms. Number-lead directionally above SN baseline too.

4. **Notification outcome language** — crime/death outcome words ("dead/died/killed/arrested/charged/sentenced") 1.26× CTR (p_adj=0.0015, n=55) — strongest notification signal post-FDR, ahead of attribution language (1.18×, p_adj=0.020). All other signals (questions, urgency, geo-anchor, superlatives, money amounts, opinion flags) not significant after FDR correction.

5. **Notification send-time** — Evening (18-20h): 0.0155 CTR vs Morning (9-11am): 0.0131. KW across time bins H=27.66, p=4.2e-5. Morning is the most-used window but worst-performing. Directional (evening n=171, night n=37).

**Existing findings extended:** Finding 5 (sports universal underperformance — extended with MSN text, MSN video completion rate, notification CTR data). Finding 4 (notification CTR declining 29% over 9 months Jun 2025→Mar 2026; monthly trend chart added).

**Key analytical notes:**
- Apple News 2026 "featured view lift" appears reversed (featured median < non-featured) due to composition: Us Weekly celebrity articles dominate the non-featured high-view pool. Within-topic, featuring still helps (weather: 2.92× lift). Not a sign that featuring stopped working.
- "What to know" 0% featuring in 2026 stable (vs 54% in 2025) — formula applied to celebrity profiles in 2026 vs weather/emergency in 2025. Content type is the driver, not formula.
- Notification channel-level CTR differences: KW p=0.051 — marginally not significant; not wired.
- SmartNews U.S. category dramatic H2 2025 decline (0.972→0.777 rank, p=7.1e-53) — directional/historical, not wired.
- Brand-level Apple News featuring variance (Charlotte Observer 88%, Modesto Bee 8%) — highly significant but not headline-actionable.

**Playbook:** 5 tiles (added MSN Formula Divergence tile).
