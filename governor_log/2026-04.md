# Governor Log — April 2026

---

### 2026-04-02 — ingest (March data) + exhaustive cross-platform analysis

**Data:** March 2026 Tarrow drop ingested. Apple News 2026 → 1,569 rows (Jan–Mar). MSN re-enabled.
Exhaustive analysis (~20 hypothesis classes) across all platforms. 5 new findings added (8→13 total).

**Signals observed:**
- MSN Formula Divergence tile was rendering empty → traced to two rigor failures (baseline key mismatch + wrong metric column). Both added to Rigor Failures Log.
- March confirmed as first solid data month; Jan–Feb had incomplete Tarrow polls.
- Finding 12 (Notification Send-Time) and Finding 13 (Notification Outcome Language) added — notification surface is rich with signal.

**Governor updates proposed:**
- [x] Added MSN baseline key mismatch to Rigor Failures Log
- [x] Added MSN wrong metric column to Rigor Failures Log
- [x] Added MSN hardcoded row lookup failure to Rigor Failures Log
- [x] Added Jan–Feb 2026 data holes to Known Data Quirks

**Applied:** Yes — incorporated into GOVERNOR.md at initial creation (2026-04-03)

---

### 2026-04-03 — stakeholder meeting: Pierce / Sarah Price (~25 min)

**Context:** First deep alignment call with Sarah Price after she received site access (2026-04-01).
Sarah had just come from a meeting with Chris and Sara Voluone about CSA testing module strategy.

**Signals observed:**
- Sarah's primary use case confirmed: actionable headline formulas for Apple News and SmartNews editorial. Team has paused syndication format variation — headline-only. → **Stakeholder Focus updated**
- Sarah wants rules she can put directly in a style guide ("if you lead with a number, it needs XYZ"). → **Confirms: avoidance rules + compound rules are high-value**
- Sarah noted Tarrow data holes — offered to co-approach Chris Tarot. → **Confirms: data quality is a shared concern, not just a pipeline issue**
- Sara Voluone has a SmartNews/Apple News formatting guide in progress — Sarah will send to Pierce. → **Active Probing Queue: format guide correlation analysis (completed same day)**
- Sarah confirmed the Experiments tab is the right home for directional findings → actionable test suggestions. → **Architecture confirmed: directional findings → experiments, not findings**
- Sarah has SEMrush API key + 250K credits; 3-way meeting planned next week. → **New workstream; not yet in governor scope**
- Sarah asked Pierce to request tile-by-tile feedback at her convenience (tiles 1–13). → **Primary calibration signal incoming**
- Cluster tags: 15 of 18 product tickets in code review. → **Instrumentation progress; watch for when variant tracking becomes available**

**Governor updates proposed:**
- [x] Stakeholder Focus section set to 2026-04 based on Sarah's confirmed priorities
- [x] Confirmed Interesting Patterns populated from this meeting + format guide analysis
- [x] Added "topic rankings without actionable hook" to Low-Signal

**Applied:** Yes

---

### 2026-04-03 — ad-hoc analysis: Platform Format Guide evidence assessment

**Context:** Sara Voluone's draft SmartNews/Apple News Platform Format Guide reviewed.
Full re-analysis run against 4,174 Apple News articles, 38,213 SmartNews articles, 1,783 notifications.
Ten analyses executed. Results delivered as .docx report for Sarah's use.

**Signals observed:**
- `what_to_know` significantly underperforms on SmartNews (p=0.046, r=−0.135). Guide recommends it. → **High-value contradiction finding — added to Confirmed Interesting**
- Questions hurt on BOTH platforms (AN: p<0.001 r=−0.265; SN: p<0.001 r=−0.091). → **Universal avoidance rule — high-value**
- Notification formula effects are 2–5× larger than views formula effects. → **Confirms notifications as the highest-signal channel**
- Apple editors over-select questions for featured (1.67×); algorithm penalizes them. → **Novel tension — confirmed interesting**
- SmartNews 80–99 char optimal bin confirmed; 100-char threshold has no statistical basis (p=0.915). → **Refines guide guidance — confirmed useful**
- Apple News curve rises through 110–119 chars; guide's 110-char ceiling is conservative. → **Refines guide guidance**
- Service journalism does NOT outperform hard news on SmartNews (p=0.74). Guide claimed it does. → **Contradiction of guide claim — confirmed interesting**
- Analysis 3 (SmartNews formula): multiple comparisons family = 7 tests, Bonferroni α/7 = 0.0071. WTK (p=0.046) does not survive Bonferroni correction. Should be reported as "directional" not "significant" in the strictest sense. → **Rigor note: report Bonferroni threshold when n of tests ≥5**

**Governor updates proposed:**
- [x] Active Probing Queue populated with 7 questions surfaced by this analysis
- [x] Rigor note on multiple comparisons added to Statistical Method Requirements
- [ ] Consider adding Bonferroni caveat to the WTK/SmartNews finding description on the site — pending

**Applied:** GOVERNOR.md updates applied. Site finding update pending.

---

### 2026-04-08 — Sarah Price tile feedback + Tracker→ANP vertical analysis

**Context:** Sarah Price provided written feedback on all 13 site tiles. Full tile-by-tile review with
recommendations executed. Tracker→ANP join run as exploratory analysis to enable trendhunter vertical
breakdown. Full governor update applied.

**Tile decisions:**
- KEEP (as-is): 1 (Featured), 2 (Push Notifications—minus timing), 3 (Platform Topic Inversion), 5 (Trends Over Time), A (Formula × Topic—restructured), B (SmartNews Formula Trap—labeled one-time setup), 8 (Bottom Performers)
- CUT: 4 (Views vs Reading Depth), 6 (Featuring Reaches Non-Subscribers), 7 (Topic Predicts Featuring—Formula Doesn't), MSN Formula Divergence tile, Push notification send-time section
- RESTRUCTURE: A (Formula × Topic)—lead with non-weather; weather = United Robots footnote

**Key signals observed:**
- Sarah's cuts are unified by a single principle: she cuts anything that doesn't produce editorial action. Platform ops metrics (send timing, subscriber reach mechanics, MSN issues) are not her lever.
- Sarah explicitly named trendhunter verticals as a priority: Mind-Body, Everyday Living, Experience — identified by author (Allison Palmer, Lauren Jarvis-Gibson, Lauren Schuster). General/Discovery (Ryan Brennan, Hanna Wickes, Samantha Agate) is a distinct group — search/discover intent, not trendhunter.
- Tracker→ANP join: 32% match rate (355/1,106 rows). 0% featuring rate across all verticals (355 articles). ANP overall baseline = 1.2% featuring. Featuring is not currently a lever for this content type.
- Trendhunter median views (Everyday Living: 159, Mind-Body: 174) slightly above General/Discovery (100) — directional, p=0.10.
- General/Discovery ceiling (53K views) driven by nature/wildlife/breaking science — not formula.
- Formula × Topic: weather artificially dominates the dataset (United Robots automated content). Non-weather signal is real: Crime + "Here's" 16% (n=89), Business + "Here's" 14% (n=72), Sports 0% regardless of formula.

**Governor updates applied this session:**
- [x] Stakeholder Focus: added trendhunter vertical mapping, featuring-not-a-lever note, push timing + MSN out of scope
- [x] Confirmed Interesting: 3 new patterns (0% featuring, Formula × Topic non-weather, nature/wildlife ceiling)
- [x] Confirmed Low-Signal: 4 new entries (Views/Reading Depth, Featuring Reaches Non-Subscribers, Push timing, MSN Formula Divergence tile, Topic Predicts Featuring—Formula Doesn't)
- [x] Active Probing Queue: 5 new HIGH/MED items; retired send-hour LOW item
- [x] What "Interesting" Means: 2 new patterns (per-vertical guidance, absence findings)
- [x] Known Data Quirks: 3 new entries (weather = United Robots, Tracker→ANP join match rate, multi-outlet duplicates)
- [x] generate_site.py: tiles cut (4, 6, 7, MSN tile suppressed), send-time section removed, Formula × Topic restructured, SmartNews tile labeled one-time setup, SHOW_MSN_TILE = False constant added
- [x] All smoke tests passing (16/16) after pipeline changes

**Applied:** Yes — GOVERNOR.md, generate_site.py, governor_log all updated.

---

### 2026-04-08 — Header structure unification + stakeholder alignment audit + programmatic guardrails

**Context:** Comprehensive alignment audit against Sarah Price's expressed needs. Six gaps identified
and resolved. New systematic guardrails added to build report to prevent recurrence.

**Gaps resolved:**
1. **MSN tile in playbook** — playbook MSN tile was gated on `msn.empty` only, not `SHOW_MSN_TILE`. Fixed: `(msn.empty or not SHOW_MSN_TILE)` now gates both main analysis tile and playbook tile consistently.
2. **Author playbook tiles labeled by name only** — Sarah navigates by content type, not author name. Fixed: tile labels now show `Author · Vertical` (e.g., "Allison Palmer · Mind-Body") using the existing AUTHOR_VERTICAL mapping.
3. **Hanna Wickes question-format recommendation contradicted platform-wide SmartNews avoidance rule** — no caveat existed. Fixed via PLATFORM_AVOIDANCE_FORMULAS guardrail: any author tile recommending `question_mark` or `what_to_know` now auto-appends a ⚠ cross-platform note.
4. **Lauren Schuster Yahoo routing recommendation from low-signal data** — Yahoo is flagged as low-confidence in GOVERNOR.md (AOL split, data discontinuity) but was being surfaced as "best platform" in tile action and guidance rows. Fixed via LOW_SIGNAL_PLATFORMS guardrail: both code paths now append ⚠ platform caveat and redirect to Apple News / SmartNews.
5. **Quote lede type finding not in style guide** — the analysis was computed and in main analysis detail panel but missing from the style guide as a writable rule. Added as "Directional" rule: "When using a quote lede, lead with official/authority quotes for highest Apple News featuring odds."
6. **Header structure inconsistent across 6 pages** — each page used different HTML structure, CSS values, and element hierarchy. Unified to grader's pattern: `.container` → `h1` (1.7em, accent, 700) → `.sub`/`.subtitle` across all pages. `eyebrow` spans removed.

**Programmatic guardrails added (prevent recurrence):**
- `PLATFORM_AVOIDANCE_FORMULAS` dict (generate_site.py) — maps formula keys to cross-platform warning text; checked at tile assembly time
- `LOW_SIGNAL_PLATFORMS` set (generate_site.py) — flags Yahoo (and future entries); both `_action` and `_guidance_rows` paths check this
- **Stakeholder scope audit** added to build report — runs on every build, checks:
  - SHOW_MSN_TILE=False → no MSN tile in playbook HTML
  - No editorial routing recommendation to LOW_SIGNAL_PLATFORMS without caveat
  - No author tile recommends PLATFORM_AVOIDANCE formula without ⚠ note
- Audit was immediately useful: caught Lauren Schuster's Yahoo routing in `_guidance_rows` (a separate code path from `_action` that bypassed the initial fix)

**Governor updates:**
- [x] Active Probing Queue: quote lede type HIGH item is now addressed (finding in style guide + main analysis)
- [ ] Consider retiring quote lede type from probing queue or moving to MED — partially answered, would benefit from more data

**Applied:** Yes — generate_site.py, generate_style_guide.py, governor_log updated.

---

### 2026-04-08 — Nav/theme parity pass (no analysis)

**Context:** Continuation of prior session. No new data or analysis. Full nav audit and parity fix
across all six site pages (main, playbook, author-playbooks, style-guide, experiments, grader).

**Changes applied:**
- Playbook + experiments nav padding: `0 2rem` → `0 28px`
- Style-guide: added `body.theme-light` CSS block (was missing), added `<body class="theme-dark">` default, replaced non-canonical JS toggle with `applyTheme(t)` / `toggleTheme()` pattern
- Grader: regenerated with canonical nav (was already correct in generator from prior session; HTML was stale)
- All 6 pages pass automated nav audit: height=44px, padding=0 28px, font=12px, backdrop-filter, moon emoji, theme-dark/light classes, body default=theme-dark

**Governor updates:** None. No analysis findings produced, no queue items changed, no new quirks.

**Applied:** Yes

---

### 2026-04-08 — Three new analyses + exhaustive Sarah Price alignment pass

**Context:** Continuation of prior session (context compaction). Following the full alignment audit
and programmatic guardrail implementation, three new analyses were added to repopulate all site pages
with maximally Sarah-relevant content. Build confirmed clean (all stakeholder scope audit checks pass).

**New analyses added to generate_site.py:**

1. **Question type deeper dive** (`classify_question_word()` + `df_question_types`)
   - Surfaced in Featured detail panel after the quote lede type table
   - Finding: most question headlines (n=162 of 178) fall into "other" — phrasing doesn't begin with a canonical question word. Named word-type buckets have n=2–5. **Underpowered; reported as illustrative only.** How questions show 67% featuring rate but n=3.
   - Note: This analysis is directional at best until mid-to-late 2026 when per-type n approaches 30.

2. **Notification CTR × character length** (`df_notif_len`, `NOTIF_LEN_BEST_BIN`, `NOTIF_LEN_BEST_CTR`)
   - Surfaced in notifications detail panel as new table
   - **Clean finding: 70–89 chars = 1.45% median CTR for news brands (n=874), p<0.05 vs. other bins**
   - Added as rule to style guide (Notification Rules section). Promoted to tier-directional on experiments page.

3. **Character length × formula interaction** (`df_char_formula`)
   - Surfaced in longitudinal/Tile 5 detail panel
   - Finding: 90–109 chars is best bin for possessive (68th %ile), quoted lede (55th), number lead (55th). Question format peaks at 70–89 (30th %ile, consistent with the avoidance finding).
   - Promoted to tier-directional on experiments page.

**Style guide updates:**
- New rule added: "When using a quote lede, lead with official/authority quotes for highest Apple News featuring odds" (Directional)
- New rule added: "Target 70–89 characters for notifications" (Directional, from analysis 2)
- Question format rule updated to reference How/Why favorability vs. Will/Can/Who underperformance

**Build verification:** All 16+ automated checks pass. Stakeholder scope audit clean. Build report confirmed: `✓ Stakeholder scope clean (MSN gate, low-signal platforms, avoidance formulas all honored across all pages).`

**Governor updates proposed and applied:**
- [x] Active Probing Queue: Quote lede type — updated to note directional finding in style guide, small n limitation flagged
- [x] Active Probing Queue: Question format deeper dive — moved to MED, noted underpowered (n=2–5 per bucket), needs mid-2026+ data
- [x] Active Probing Queue: Char length × formula — moved to MED, marked as run (2026-04-08), directional
- [x] Active Probing Queue: Notification CTR × char length — moved to MED, marked as run (2026-04-08), clean finding
- [x] Known Data Quirks: Added `classify_question_word()` bucket n limitation
- [x] GOVERNOR.md last updated timestamp updated to 2026-04-08

**Applied:** Yes — GOVERNOR.md, governor_log updated. Site generated and verified clean.

---

### 2026-04-08 — Gap closure: 4 remaining explicit/implicit asks + probing queue compliance guardrail

**Context:** Audit revealed the prior session left 4 outstanding items from Sarah's expressed needs
despite declaring alignment. All addressable gaps closed this session. One blocked item documented.

**Analyses added:**

1. **Nature/Wildlife dual-headline formula guide** (Finding 3 detail panel)
   - Apple News NW: n=287 total articles; SmartNews 2025 NW: n=475.
   - Per-formula n too small for significant tests (most formulas <20 NW articles). All results directional.
   - Key direction: possessive and "Here's" lead on AN within NW; question format lowest.
   - On SN, any formula is acceptable given NW's strong algorithmic boost (nw_sn_idx > 1.0).
   - Dual-headline table added to Platform Topic Inversion detail panel with caveat.

2. **0% Featuring investigation** (author-playbooks page)
   - All 247 matched articles have section tags — section tagging is NOT the driver.
   - 0% featuring is driven by content type/topic category — lifestyle/health/wellness content is not Apple News editorial priority regardless of formula or metadata.
   - Investigation table surfaced on author-playbooks page; editors now have a clear answer: there's no metadata fix, content type is the constraint.

3. **Head-to-head headline examples** (Trends Over Time detail panel)
   - Top and bottom organic Apple News headlines by formula (2025–2026 pooled).
   - 5 formulas covered: "Here's", possessive, question, number lead, quoted lede.
   - Top = highest organic views (non-featured where flag available). Bottom = lowest among ≥100-view articles.
   - Direct editorial utility: editors can see what a high-performing possessive headline looks like vs. what a low-performing one looks like, in actual text.

4. **Trendhunter notification vertical breakdown** — BLOCKED
   - Notification data (Apple News Notifications sheet) has no author column — only Channel/brand.
   - No reliable join path to Tracker author→vertical without fuzzy text matching.
   - Documented in probing queue as BLOCKED; will unblock when Tarrow adds author attribution.

**Programmatic guardrail added:**
- `PENDING_HIGH_ANALYSES: list[str]` constant added to generate_site.py after policy constants.
- Build report compliance check: if list is non-empty, each item emits a scope warning.
- Creates a forcing function: any HIGH probing queue item with available data must either be implemented (and removed) or explicitly documented as blocked/MED.
- Currently empty — all HIGH items are either run or blocked with documented reason.

**Governor updates applied:**
- [x] Active Probing Queue: Nature/Wildlife moved to MED (run, directional, small n)
- [x] Active Probing Queue: 0% featuring investigation marked Complete
- [x] Active Probing Queue: Trends Over Time examples marked Complete
- [x] Active Probing Queue: Trendhunter notification vertical marked BLOCKED (data limitation)
- [x] Build verified clean: all checks pass, stakeholder scope audit ✓, 0 new warnings

**Applied:** Yes — generate_site.py, GOVERNOR.md, governor_log all updated.
---

### 2026-04-08 — April 2026 ingest + new SmartNews channel data integration

**Context:** New data file received (`Top Stories 2026 Syndication.xlsx`, dated 2026-04-08).
SmartNews sheet expanded from 7 to 28 columns. MSN sheet renamed. Full pipeline update applied.

**Data changes in new export:**
- Apple News: 3,884 → 4,283 rows (+399, March 2026 data added)
- Notifications: 1,783 → 1,923 rows (+140)
- SmartNews: 37,695 rows unchanged (2025 file not updated)
- MSN sheet renamed from "MSN (minumum 10k PV)" → "MSN" (typo fixed by Tarrow)
- SmartNews 2026 expanded: 7 → 28 columns; new: recommended_view, per-channel views
  (Top, Entertainment, Lifestyle, U.S., Business, World, Technology, Science, Health, Local, Search),
  social shares (twitter, facebook, evernote, pocket, line); `date` dropped, replaced by `month`

**Code changes:**
- MSN sheet name updated in `_KNOWN_SHEETS_2026` and `read_excel` call
- `CATS_COMMON` updated: added `"Search"` (new SN channel)
- `recommended_view` handling added to sn26 prep block
- SmartNews channel distribution analysis added: `df_sn_channels`, `SN_TOP_SHARE`, `SN_RECOMMENDED_SHARE`
- Channel distribution table surfaced in SmartNews Formula Trap detail panel
- Latent `_fmt_p` ordering bug fixed (was silent when MSN excluded; triggered on re-inclusion)
- New repo root data file replaced with new version

**Channel distribution findings (T1 content, Jan–Mar 2026, n=217 articles):**
- Top feed: 86.3% of views — dominant, formula strategy should optimize for Top
- Entertainment: ~10% — meaningful for womansworld.com / entertainment content
- Local: 0.6%, Health: 0.4% — non-trivial, relevant for trendhunter verticals
- recommended_view: 0.5% of article_view — too small to analyze yet

**Bugs fixed:**
- `_fmt_p` function defined after call site — silent when MSN excluded, raised NameError on MSN re-inclusion. Moved definition before first use.

**Governor updates applied:**
- [x] Known Data Quirks: added `recommended_view` small-signal note
- [x] Known Data Quirks: added platform re-inclusion latent bug warning
- [x] Active Probing Queue: added SN channel × formula item (MED)
- [x] Build verified clean: Apple News 4,283 rows, Notifications 1,923, all checks pass

**Applied:** Yes — generate_site.py, GOVERNOR.md, governor_log, data file all updated.

---

### 2026-04-09 (session 2) — Grader platform-aware scoring + adapter pattern planning + stakeholder documentation

**Context:** No new data or analysis this session. Three workstreams: (1) grader implementation gap closed, (2) architecture planning for team-wide adapter pattern per Chris Palo directive, (3) stakeholder documentation produced for Chris Palo covering all three built artifacts.

**Grader platform-aware scoring:**
- Identified that Tracker columns H (`Syndication platform`), I (`Headline`), and R (`Primary Keywords`) were already present and available — previous assessment incorrectly called these data gaps. Headline and keywords were already being read correctly. Platform was not.
- `_parse_platform()` added to `generate_grader.py`; normalizes `Apple` → `apple_news`, `Smart News` → `smart_news`, etc.
- `_char_count()` now scores against platform-specific target: Apple News 90–120, SmartNews 70–90, unknown falls back to 70–120 (passes if valid for either).
- `_number()` note is now platform-aware: "positive SmartNews signal" / "caution: underperforms on Apple News" / generic for unknown.
- Platform badge added to each headline card.
- 19/19 smoke tests pass.

**Adapter pattern (Chris Palo directive):**
- Architecture decision: adapter surface is GOVERNOR Part 1 (relevance), not code. No code changes needed for full team rollout.
- Four files to build off-hours: `GOVERNOR_CORE.md`, `GOVERNOR_SARAH.md`, `ADAPTER_TEMPLATE.md`, `README_ADAPTER.md`.
- CLAUDE.md adapter-loading logic defined: load `GOVERNOR_CORE.md` + `ADAPTER.md` if present; else fall back to `GOVERNOR.md`.
- Three output modes documented: interactive session, narrative memo, site tile.
- Recorded in CONTEXT.md open items and memory.

**Stakeholder documentation:**
- Three "one-sheet dummies guides" written for Chris Palo / Sara Vallone audience.
- Each covers: what it is, how it gets data, how it processes data, what keeps it honest (guardrails), how it gets better over time, how outputs are prioritized by stakeholder interest.
- Analytics pipeline doc includes full Governor explanation and continuous improvement story.

**Governor updates applied this session:**
- [x] Known Data Quirks: Tracker `Syndication platform` short-form values added
- No probing queue changes — no analysis run, no new questions surfaced
- No low-signal changes
- No rigor failures

**Applied:** Yes — GOVERNOR.md, CONTEXT.md, governor_log, generate_grader.py all updated.

---

### 2026-04-10 — Cluster/variant production section; no actionable findings

**Context:** Continuation from prior session. No new data. Added Tracker-level cluster production analysis to author-playbooks page and propagated `cluster_id` through the join pipeline.

**What was built:**
- `_cluster_production_section()` added to `generate_site.py` — runs on `tracker_raw` directly (no performance join). Shows total clusters, share multi-variant, mean/median/max cluster size, distribution table, author breakdown.
- `_cluster_id` computation propagated through `_t_cols` → `_t_extra_cols` → all three platform join `rows.append` dicts.
- 3 new `_COL_TOOLTIPS` entries for cluster section headers. 19/19 tests pass, build clean.

**Findings assessment:**
- Production section: operational inventory only. Ryan Brennan (154 clusters, 3.15 mean), Hanna Wickes (144, 3.15) produce 71% of all clusters. Lauren J-G (40, 2.0), Allison Palmer (19, 1.95) produce fewer variants per cluster.
- Performance join: Mann-Whitney p=0.635, no detectable difference between originals and variants. n=37 matched articles — underpowered, no conclusions possible.
- **No actionable findings surfaced.**

**Data quality flag (new):**
- "Hanna Wickes" and "Hanna WIckes" (capital I in WIckes) are two separate rows in Tracker — 144 + 4 = 148 clusters that should be one person. Will mis-split author playbook. Tarrow/Sara should fix in source data.

**Governor updates:**
- No probing queue changes — no new signals, no new questions.
- No rigor failures.
- Data quality flag noted above; not added to Known Data Quirks (it's a source data typo, not a pipeline behavior).

**Applied:** Yes — CONTEXT.md, governor_log updated.
